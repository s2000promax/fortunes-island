import { TableCoordinates } from 'entities/InteractiveTable';
import { BitsNumbers } from 'entities/InteractiveTable/model/types/interactiveTable';

export const useGetHovers = (hoverId: BitsNumbers, TableBitsButtonsArray: Array<TableCoordinates>) => {

  return {};
};
